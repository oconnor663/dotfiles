#! /usr/bin/env python3

import pprint
import sys
import umsgpack

# sys.stdin is buffered by default, and there's no way to override that.
# Instead, we rewrap stdin in our own file object.
stdin = open(sys.stdin.fileno(), mode='rb', buffering=0)

pprint.pprint(umsgpack.unpack(stdin))
